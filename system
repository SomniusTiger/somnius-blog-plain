// tt = title, bb = brief, md = copy markdown file to webroot, mv = create redirect for argument, cp = copy directory // = comment (skip)

// tt What is a system?
// bb It's a way for one body to have multiple internal parts that work together. At least, that's how it works for me. See this post for more info.
// md wiki/system/system-intro.md
